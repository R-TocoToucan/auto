---
quick_id: 260912-j54
mode: quick
type: execute
autonomous: true
requirements: [SCOPE-5]
files_modified:
  - src/bithumb_bot/cli/handlers/paper_run.py
  - tests/cli/test_handlers_paper_run.py
depends_on: [260912-erv]
must_haves:
  truths:
    - "`bt paper run` against a dataset whose already-processed forward prefix has been byte-mutated exits 1 with a clean `bt paper run: refusal (ProcessedPrefixMutatedError): <offending open_time_utc>` line on stderr — NO uncaught Python traceback, NO new report file, NO mutation of `state.json` / `candle_fingerprints.jsonl` / `fills.jsonl` / `signals.jsonl`."
    - "The paper-session refusal branch treats `ProcessedPrefixMutatedError` uniformly with `PaperStateDirError` / `ForwardDatasetDivergenceError` / `FillReplayDivergenceError` / `SidecarHashMismatchError` — same shared formatter, same exit code, same stderr shape."
    - "The gap flagged by `260912-erv-VERIFICATION.md` under `## Human Verification Required` is closed."
  artifacts:
    - "`src/bithumb_bot/cli/handlers/paper_run.py`: import of `ProcessedPrefixMutatedError` from `bithumb_bot.errors`; the exception name added to the existing paper-session refusal `except (...)` tuple (currently `PaperStateDirError, ForwardDatasetDivergenceError, FillReplayDivergenceError, SidecarHashMismatchError`, wrapping the `run_paper_session(...)` call around lines 444-462)."
    - "`tests/cli/test_handlers_paper_run.py`: one new test class `TestPaperRunProcessedPrefixMutationRefused` with one method proving the clean exit-1 refusal on a mutated processed-prefix resume."
  key_links:
    - "The paper handler's `except (...)` tuple → the runner's raise site: `bithumb_bot.paper.runner._verify_processed_prefix_fingerprints` raises `ProcessedPrefixMutatedError` with the offending candle `open_time_utc` embedded in the message (verified: this is where the message is produced; DO NOT touch runner)."
    - "The shared refusal formatter `f\"bt paper run: refusal ({type(exc).__name__}): {exc}\"` (paper_run.py:459) already interpolates both the exception class name AND the message — adding the new type to the tuple is sufficient; NO new message branch or dispatch is required."
---

<objective>
Close the one-line CLI wiring gap left open by quick task 260912-erv: `src/bithumb_bot/cli/handlers/paper_run.py`'s `handler()` does not catch `ProcessedPrefixMutatedError` in its paper-session refusal `except (...)` tuple, so a `bt paper run` invocation against a dataset with a byte-mutated processed prefix surfaces as an uncaught Python traceback rather than the same clean `bt paper run: refusal (...)` exit-1 used for every other paper-session integrity error. Add the exception to the tuple; add one CLI test proving the clean refusal and fail-closed on-disk state.

Purpose: uniform CLI-level error handling across all four paper-session integrity refusals; a real-operator invocation of `bt paper run` can never trip an uncaught Python traceback on this refusal path.

Output: two-file diff (1 handler line + 1 import + 1 CLI test method), one local commit, no push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260912-erv-fix-two-correctness-defects-in-the-paper/260912-erv-VERIFICATION.md
@.planning/quick/260912-erv-fix-two-correctness-defects-in-the-paper/260912-erv-SUMMARY.md
@src/bithumb_bot/cli/handlers/paper_run.py
@src/bithumb_bot/errors.py
@tests/cli/test_handlers_paper_run.py
@tests/paper/test_resume_prefix_integrity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend paper_run.py handler except-tuple to catch ProcessedPrefixMutatedError + add one CLI adversarial test proving clean exit-1 refusal</name>
  <files>
    src/bithumb_bot/cli/handlers/paper_run.py
    tests/cli/test_handlers_paper_run.py
  </files>
  <behavior>
    - Test (new, `TestPaperRunProcessedPrefixMutationRefused::test_mutated_forward_candle_on_resume_refuses_cleanly`):
      1. Golden-path run 1: seed `state_dir/` from `_write_dataset(tmp_path)` (reuse existing helper — WARMUP_CANDLE_COUNT flat warmup candles + 1 LONG-triggering candle at `paper_start` + 1 flat candle at `paper_start + STEP`). Assert `rc1 == 0`, `state_dir/state.json` exists, `state_dir/candle_fingerprints.jsonl` exists with 2 lines (one per forward candle).
      2. Snapshot pre-invocation bytes: `state.json`, `candle_fingerprints.jsonl`, `fills.jsonl`, and (if present) `signals.jsonl`.
      3. Build a MUTATED dataset written to a DIFFERENT path (e.g., `dataset_mutated.json`, sidecar goes with it — do NOT overwrite the run-1 dataset's sidecar): identical to the run-1 dataset EXCEPT the LAST forward candle's `high` is bumped by 1 (e.g., "200000000" → "200000001"). Snapshot + config + state_dir are reused unchanged; the second `--out` points to a NEW path (`report2.json`) so overwrite-guard is not triggered.
      4. Assert `rc2 == 1` (clean refusal, NOT an uncaught exception).
      5. Read `capsys.readouterr()`; assert `err` contains the substring `"ProcessedPrefixMutatedError"` AND contains the offending candle's ISO-8601 `open_time_utc` (the last forward candle's timestamp).
      6. Assert `err` contains NO uncaught-traceback marker (the standard `"Traceback (most recent call last):"` header emitted by CPython for unhandled exceptions must be absent).
      7. Assert `report2.json` was NOT created AND `report2.json.sha256` was NOT created (no report on refusal).
      8. Assert `state.json`, `candle_fingerprints.jsonl`, `fills.jsonl`, and `signals.jsonl` (if it was present pre-invocation) are BYTE-IDENTICAL to their pre-invocation snapshots (fail-closed: NO new audit-trail line appended on the refusal path).
    - Rationale for mutating `high` on the LAST forward candle (index `WARMUP_CANDLE_COUNT + 1`, the second forward candle): `_verify_processed_prefix_fingerprints` runs BEFORE `_assert_prefix_replay` in the runner (per `260912-erv-VERIFICATION.md`'s Key Link table), so `ProcessedPrefixMutatedError` fires first regardless of whether the mutation would also perturb the derived fill/signal stream. The `WARMUP_CANDLE_COUNT + 1` candle's `high` is a safe field to bump: it does not change the warmup slice (which ends at index `WARMUP_CANDLE_COUNT - 1`), does not change `paper_start_ts_utc` (which is `candles[WARMUP_CANDLE_COUNT].open_time_utc`), and therefore does not trip `_resume_divergence_check`'s `ForwardDatasetDivergenceError` first.
    - Test placement: add a NEW class `TestPaperRunProcessedPrefixMutationRefused` at the bottom of `tests/cli/test_handlers_paper_run.py`, after `TestPaperRunHelp`. Reuse the existing module-level `_env` fixture, `_write_dataset`, `_write_snapshot`, `_write_config`, and `_run` helpers unchanged. If a mutated-dataset helper is needed, add ONE small local helper (either a `_write_dataset_with_mutated_last_high(tmp_path, *, name)` module-level function that duplicates the existing `_write_dataset` with `high="200000001"` on the last candle, OR extend `_write_dataset` with a defaulted `mutate_last_high: bool = False` kwarg — either is acceptable; pick the smaller diff). DO NOT introduce a new base class, a shared refusal-matrix parametrization, or a helper module.
  </behavior>
  <action>
    Step A — Extend the handler except-tuple (single-file edit to `src/bithumb_bot/cli/handlers/paper_run.py`):

    1. Add `ProcessedPrefixMutatedError` to the existing `from bithumb_bot.errors import (...)` block (alphabetical placement inside the parentheses — currently the imports are `FillReplayDivergenceError, ForwardDatasetDivergenceError, PaperStateDirError, SidecarHashMismatchError`; `ProcessedPrefixMutatedError` sorts between `PaperStateDirError` and `SidecarHashMismatchError`).
    2. Add `ProcessedPrefixMutatedError,` as one line inside the paper-session `except (...)` tuple that currently wraps the `run_paper_session(...)` call (the tuple lives around lines 452-457, alongside `PaperStateDirError`, `ForwardDatasetDivergenceError`, `FillReplayDivergenceError`, `SidecarHashMismatchError`). Alphabetical placement inside the tuple. The refusal branch body is unchanged — the shared formatter `f"bt paper run: refusal ({type(exc).__name__}): {exc}"` already interpolates both the exception class name AND the runner-provided message (which itself embeds the offending candle `open_time_utc`), so NO new branch, dispatch, message template, or helper is needed. Add the exception to the tuple — nothing more.
    3. Do NOT re-order or reformat the existing entries in either the import block or the except-tuple; add exactly one line to each and let ruff format if it insists (it will not — both are already vertical/one-per-line).

    Step B — Add ONE new CLI adversarial test (single-file edit to `tests/cli/test_handlers_paper_run.py`) implementing the `<behavior>` block above:

    1. If needed, add a small module-level `_write_dataset_with_mutated_last_high(tmp_path: Path, *, name: str = "dataset_mutated.json") -> Path` helper next to the existing `_write_dataset` — duplicate the existing helper's body verbatim EXCEPT set the last forward candle's `high` to "200000001" instead of "200000000". (Alternative: add `mutate_last_high: bool = False` to `_write_dataset` and gate the one line; pick whichever produces the smaller diff.) DO NOT touch `_write_snapshot`, `_write_config`, `_env`, or `_run`.
    2. Append a new class `TestPaperRunProcessedPrefixMutationRefused` at the END of the file (after `TestPaperRunHelp`) containing one method `test_mutated_forward_candle_on_resume_refuses_cleanly(self, _env, tmp_path, capsys)` that implements the 8-step assertion sequence from `<behavior>`. Use `main([...])`-based dispatch via the existing `_run(...)` helper — do NOT introduce a subprocess-based invocation (in-process is faster, cleaner, and matches every other test in the file).
    3. The assertion on the presence of a Python-traceback marker in stderr should NOT hard-code the CPython header string in a comment or docstring that could confuse future greps — express it as `assert "Traceback" not in err` (a substring absence check on the runtime stderr string is sufficient; if the handler catches the exception, CPython emits no traceback header at all, so any substring of that header suffices).
    4. Do NOT modify the existing test classes or helpers.

    Step C — Full validation gates + one local commit (see `<verify>` block below). DO NOT push. DO NOT touch any file under `src/bithumb_bot/paper/`, `src/bithumb_bot/errors.py`, `src/bithumb_bot/strategy/`, `src/bithumb_bot/backtest/`, `src/bithumb_bot/execution/`, `src/bithumb_bot/broker/`, or anywhere else. Diff must be exactly two files: `src/bithumb_bot/cli/handlers/paper_run.py` and `tests/cli/test_handlers_paper_run.py`.

    Commit message: `fix(cli/paper_run): catch ProcessedPrefixMutatedError in handler except-tuple`. Include body noting this closes the follow-up gap flagged in `.planning/quick/260912-erv-fix-two-correctness-defects-in-the-paper/260912-erv-VERIFICATION.md` under `## Human Verification Required`.

    DO NOT push. Local commit only.
  </action>
  <verify>
    <automated>
      # 1. New CLI test file — the new adversarial test PLUS the pre-existing golden-path/overwrite-guard/missing-flag/no-creds/hysteresis-pin/help tests must all pass.
      uv run pytest tests/cli/test_handlers_paper_run.py -q

      # 2. Paper runner regression — the 12 D2 adversarial tests + persistence + parity tests must remain green (this task must not perturb the runner at all).
      uv run pytest tests/paper/ -q

      # 3. Full-suite regression — total pass count must equal the 260912-erv baseline (829 passed, 1 skipped) PLUS exactly one new test (830 passed, 1 skipped). Any other delta is a regression to investigate.
      uv run pytest -q

      # 4. Static typing — the handler edit is import + one line inside an existing tuple; mypy --strict must remain clean.
      uv run mypy --strict src/bithumb_bot/cli/handlers/paper_run.py

      # 5. Lint — both edited files must be ruff-clean.
      uv run ruff check src/bithumb_bot/cli/handlers/paper_run.py tests/cli/test_handlers_paper_run.py

      # 6. Import boundary — trivially preserved (no new cross-module edges), but re-verify since the plan touches an import block.
      uv run lint-imports

      # 7. Diff-scope discipline — exactly two files touched.
      git diff --stat HEAD -- src/ tests/  # must show ONLY src/bithumb_bot/cli/handlers/paper_run.py and tests/cli/test_handlers_paper_run.py

      # 8. Non-modification discipline — the runner, state module, errors module, strategy, backtest, execution, and broker must all be untouched.
      git diff --name-only HEAD -- src/bithumb_bot/paper/ src/bithumb_bot/errors.py src/bithumb_bot/strategy/ src/bithumb_bot/backtest/ src/bithumb_bot/execution/ src/bithumb_bot/broker/  # must be empty
    </automated>
  </verify>
  <done>
    - `src/bithumb_bot/cli/handlers/paper_run.py` imports `ProcessedPrefixMutatedError` from `bithumb_bot.errors` and includes it in the paper-session refusal `except (...)` tuple. No other change to the file.
    - `tests/cli/test_handlers_paper_run.py` has one new test class `TestPaperRunProcessedPrefixMutationRefused` with one method proving: `rc == 1`, stderr contains `"ProcessedPrefixMutatedError"` and the offending `open_time_utc`, stderr contains no uncaught-traceback marker, no report/sidecar written, `state.json` + `candle_fingerprints.jsonl` + `fills.jsonl` + (`signals.jsonl` if present) byte-identical to pre-invocation snapshots.
    - All eight verification gates above pass (targeted CLI test, paper regression, full suite = 830 passed / 1 skipped, mypy --strict, ruff, lint-imports, two-file diff scope, empty non-modification diff).
    - Exactly one local commit whose message body cites `260912-erv-VERIFICATION.md`'s `## Human Verification Required` item as the gap being closed. NOT pushed.
  </done>
</task>

</tasks>

<verification>
The full validation matrix lives inside the single task's `<verify>` block (eight gates: targeted CLI pytest, paper-suite regression, full-suite regression, mypy --strict, ruff, import-linter, two-file diff scope, empty non-modification diff). The task is atomic — pass all eight or the commit is not made.

Behavioral spot-check the executor must run before committing (in addition to the automated suite):

```
uv run pytest tests/cli/test_handlers_paper_run.py::TestPaperRunProcessedPrefixMutationRefused -v
```

Confirm the new test passes AND its stderr assertion (captured by `capsys`) actually contains both the `"ProcessedPrefixMutatedError"` class-name substring and an ISO-8601 timestamp — a false-positive (the test passing because the test itself is buggy rather than because the fix works) can be ruled out by temporarily commenting the handler edit and re-running the test to confirm it FAILS with an uncaught `ProcessedPrefixMutatedError` propagating out of `main()` (i.e., the test correctly discriminates between fixed and unfixed handler). Re-apply the handler edit before committing. This "invert-to-confirm" step is a spot-check only — do NOT commit the inverted state.
</verification>

<success_criteria>
- `bt paper run` against a mutated-processed-prefix dataset returns exit 1 with a `bt paper run: refusal (ProcessedPrefixMutatedError): ...` stderr line and NO uncaught Python traceback.
- No new report file or sidecar is written on the refusal path; `state.json`, `candle_fingerprints.jsonl`, `fills.jsonl`, and `signals.jsonl` are byte-identical before and after the failed invocation.
- Full-suite delta is exactly +1 test (830 passed, 1 skipped vs. the 260912-erv baseline of 829 passed, 1 skipped).
- mypy --strict, ruff, and lint-imports all clean.
- Diff touches exactly two files: `src/bithumb_bot/cli/handlers/paper_run.py` and `tests/cli/test_handlers_paper_run.py`. Nothing else.
- One local commit; NOT pushed.
- The `## Human Verification Required` gap in `260912-erv-VERIFICATION.md` is closed (a subsequent verifier reading both files should see this task's commit as the resolution).
</success_criteria>

<output>
Create `.planning/quick/260912-j54-extend-paper-run-py-handler-except-tuple/260912-j54-SUMMARY.md` when done.
</output>
